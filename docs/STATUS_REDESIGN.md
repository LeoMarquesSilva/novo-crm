# Status do Redesign V2 — CRM BP

> **Como usar este arquivo:** este é o log vivo do redesign visual (Design System V2 / Corporate Clean SaaS). Antes de começar qualquer tarefa, leia a seção "Snapshot atual". Ao terminar uma fase, uma etapa relevante ou tomar uma decisão que afete outros agentes, **atualize este arquivo** (adicione uma entrada no changelog abaixo, não apague o histórico). Isso substitui precisar reexplicar contexto a cada terminal novo ou sessão reiniciada.

Responsável por manter atualizado: **Claude — Arquiteto/Lead (Maestro)**. Os demais agentes (Codex, Cursor/Grok) devem ler antes de iniciar e podem propor atualizações via `maestri ask` ao Lead.

Documento-fonte: `DESIGN_SYSTEM_V2_CRM_BP.md` (raiz do projeto) — é a especificação completa e não deve ser reescrita; este arquivo só registra progresso e decisões.

---

## Snapshot atual

*(Última atualização: 18/09/2026)*

- **Fase atual:** Fase 6 — Demais módulos — **concluída**. Lote 1 (Clientes/Login/Perfil/Notificações), lote 2 (Dashboard) e lote 3 (Documentos/Due diligence) concluídos. Dentro de Administração: lote A (Campos/Cláusulas), lote B (Usuários), lote D1 (Integrações não-D4Sign), lote C1 (Proposta-escopo/Catálogo), lote C2 (Proposta-escopo/Importação, `scope-import/`) e lote D2 (D4Sign + Admin/Documentos, fecha a fase) concluídos — todos pelo Claude — Lead, sem Cursor/Grok/Codex conectados nesta sessão.
- **Fases concluídas:** Fase 0 — Auditoria; Fase 1 — Fundação de tokens; Fase 2 — Primitives; Fase 3 — Shell; Fase 4 — Leads/Kanban; Fase 5 — Propostas/Contratos; Fase 6 — Demais módulos
- **Próxima fase:** Fase 7 — Limpeza (remoção de tokens legados, Inter, estilos locais órfãos)
- **Bloqueios conhecidos:** ver seção "Bloqueios" abaixo

## Fases (referência rápida — ver seção 33 do documento-fonte para detalhes completos)

- [x] Fase 0 — Auditoria
- [x] Fase 1 — Fundação (tokens, cores, tipografia, raio, sombra, motion, z-index)
- [x] Fase 2 — Primitives (Button, Input, Textarea, Select/CrmSelect, Switch, Tabs, Badge, Card/Surface, Dialog/AlertDialog/Popover/Tooltip, Table/Skeleton/Progress/Alert, DateInputBr/TimeInputBr/CalendarBr). Checkbox/RadioGroup: n/a (não existem no repo). Avatar/Label: revisados, sem alteração
- [x] Fase 3 — Shell e padrões globais (AppShell incl. sidebar 72/248px e navegação mobile, PageHeader V2, Surface). Toolbar/EntityHeader: sem componente genérico ainda — criados quando a Fase 4/5 tiver um consumidor real
- [x] Fase 4 — Kanban de Leads / Novo Lead / shell do detalhe (`CrmEntityHeader`, novo componente)
- [x] Fase 5 — Propostas / Contratos (conteúdo das tabs; chrome novo `document-builder-chrome.tsx`). Hub `/crm/contratos` fora do fluxo do lead: não migrado
- [x] Fase 6 — Demais módulos (Dashboard, Clientes, Due diligence, Administração, Login) — Clientes/Login/Perfil/Notificações (lote 1), Dashboard (lote 2), Documentos/Due diligence (lote 3) concluídos; dentro de Administração, lote A (Campos/Cláusulas), lote B (Usuários), lote D1 (Integrações não-D4Sign), lote C1 (Proposta-escopo/Catálogo), lote C2 (Proposta-escopo/Importação) e lote D2 (D4Sign + Admin/Documentos) concluídos — fase encerrada
- [ ] Fase 7 — Limpeza (remoção de tokens legados, Inter, estilos locais órfãos)

## Decisões registradas

| Data | Decisão | Responsável | Motivo |
|---|---|---|---|
| 14/09/2026 | Commitar separadamente 15 mudanças não commitadas em proposta/contrato (regra de negócio) antes de iniciar trabalho visual | Claude (Lead) | Isolar mudanças funcionais pré-existentes do trabalho de redesign, evitando misturar histórico |
| 14/09/2026 | Fase 1: **não** sobrescrever a escala de raio legada (`--radius-sm/md/lg/xl/2xl/3xl/4xl`, calculada a partir de `--radius`) nem `shadow-sm/md/lg` nativos do Tailwind. Escala V2 entra sob nomes novos e não-colidentes: `--radius-v2-sm/md/lg/xl/2xl/full` e `--shadow-v2-xs/sm/md/lg`, como custom properties soltas em `:root` (fora de `@theme inline`), consumíveis via `rounded-[var(--radius-v2-lg)]` / `shadow-[var(--shadow-v2-md)]` | Claude (Lead) | A escala legada tem 482 usos em 99 arquivos; o Badge depende de `rounded-4xl` pra ficar pill-shaped. Sobrescrever direto quebraria isso antes da Fase 2 revisar cada primitive. Ver `DESIGN_TOKENS_CHEATSHEET.md` |
| 14/09/2026 | Mesma lógica para a escala tipográfica V2: classes `.text-v2-display-sm`, `.text-v2-heading-xl` etc. (prefixo `v2`), para não colidir com `.heading-xl`/`.heading-lg` já em uso (login, dashboard, clientes) | Claude (Lead) | Evitar recolorir telas ainda não migradas (Fases 4/6) |
| 14/09/2026 | Tokens de texto semântico (`--text-primary`, `--text-secondary`, `--text-muted`, `--text-placeholder`, `--text-disabled`) ficam como custom properties soltas em `:root`, **fora** do bloco `@theme inline`; o alias de utility Tailwind usa chave própria (`--color-text-primary-v2` etc.) apontando pro valor canônico | Claude (Lead), revisado por Codex | Dentro de `@theme`, um `--text-primary` bruto colide com o namespace `--text-*` de tamanho de fonte do Tailwind v4 e geraria um utilitário `text-primary` concorrente com o que já vem de `--color-primary` |
| 14/09/2026 | Tokens semânticos shadcn (`--background`, `--primary`, `--border`, `--input`, `--ring`, `--destructive`, paleta de gráficos) foram sobrescritos direto pros valores V2 já na Fase 1, sem alias temporário | Claude (Lead) | Blast radius mapeado por grep antes da troca. Correção (apontada pela Codex): a estilização pesada de Button/Card/sidebar (gradiente teal, `.glass-card`, pill, navy) continua vindo de classes/cores próprias (`--accent-teal`, `--primary-dark`, `--sidebar-*`) intocadas — mas há consumo semântico direto real desses tokens em Button (`primary-foreground`, `secondary`, `destructive`), Card (`card-foreground`), e `--border` alcança globalmente via `* { @apply border-border }` e as 682 utilities `border`/`border-x/y/t/r/b/s/e` em 110 arquivos. Risco avaliado como baixo mesmo assim: valores V2 próximos dos legados nesses pontos (cores de texto/estado, não a decoração visual pesada) |
| 14/09/2026 | Motion (`--motion-fast/default/slow/ease`) e z-index (`--z-base`…`--z-drag-overlay`) definidos na Fase 1 mas **não aplicados** a nenhum componente ainda; z-index hardcoded atual (40/50/60/70/100/120/130/400) permanece intacto | Claude (Lead) | Migração de z-index precisa ser um lote único coordenado (Fase 3 — Shell/overlays), não pontual, pra não quebrar empilhamento de overlays |
| 14/09/2026 | Fase 2, lote 1 (Button/Input/Select+CrmSelect/Badge/Card/Dialog+AlertDialog): todas as props/API existentes preservadas (`variant`/`size` do Button, `size` do Select etc.) — só as classes Tailwind internas mudaram. Button: `rounded-full`→`rounded-[var(--radius-v2-lg)]`(10px), removido hover/active translate, `teal` e `cta` passam a renderizar como `primary` (nomes mantidos por compat., 26+8 consumidores intocados), `secondary` virou `bg-white` (era `bg-secondary`), `inverse` (variante oficial nova) e `hero` (legada) usam branco-sobre-navy, disabled ganhou fundo neutro + texto disabled em vez de só opacidade, `icon`/`icon-sm`/`icon-lg` foram para 36/32/40px (V2), size `control`=36px é novo. Input/Select: 40px (36px em `data-size=sm`), raio 8px, foco azul. Card: parou de usar `.glass-card` (classe global, ainda usada por outros 15 consumidores intocados) e passou a ter raio/borda/fundo próprios sem sombra. Dialog/AlertDialog: conteúdo opaco (sem blur/translucidez), raio 16px, `shadow-v2-lg` | Claude (Lead), revisado por Codex | Reduzir risco: qualquer um dos ~700 call-sites desses componentes continua funcionando sem alteração de código |
| 14/09/2026 | `--text-placeholder` mudou de `#98A2B3` (neutral-400, ~2,58:1 sobre branco) para `#667085` (mesmo valor de `--text-secondary`/neutral-500, ~4,98:1) — desvio deliberado do valor literal do documento-fonte | Claude (Lead), achado bloqueante da Codex | `#98A2B3` não atinge WCAG AA (4,5:1) para texto normal e §27.1 exige AA; só ficou um problema real quando Input/Select passaram a consumir o token de fato, na Fase 2. `--text-disabled` manteve `#98A2B3` (WCAG isenta componente desabilitado do requisito) |
| 14/09/2026 | **Todas** as ocorrências de `rounded-[var(--radius-v2-*)]`/`shadow-[var(--shadow-v2-*)]` (27, em 15 arquivos, incluindo os já commitados no lote 1) trocadas pela sintaxe curta do Tailwind v4 `rounded-(--radius-v2-*)`/`shadow-(--shadow-v2-*)` | Claude (Lead), achado bloqueante da Codex | `tailwind-merge` não reconhece de forma confiável duas classes `[var(--x)]` (bracket+var()) como conflitantes entre si — um `className` de consumidor com um `shadow-[...]`/`rounded-[...]` próprio ficava "empatado" com o do primitive nas duas classes convivendo no DOM, e o CSS compilado decidia por ordem de declaração (o primitive podia vencer silenciosamente, sem erro nenhum). A sintaxe `(--x)` é reconhecida corretamente. Regra para daqui pra frente: **sempre usar `rounded-(--token)`/`shadow-(--token)`, nunca `rounded-[var(--token)]`**, para qualquer arbitrary value que referencie uma custom property |
| 14/09/2026 | Tabs: variante `segmented` (§20.1) adicionada como alias visual de `default` (mesmas classes); raio do container 8px→10px (radius-v2-lg) e do trigger 14px legado→8px (radius-v2-md) | Claude (Lead), achado da Codex | `segmented` era o único valor de variant do documento-fonte ausente na implementação |
| 14/09/2026 | Table: `TableRow` ganhou `h-11` (44px) explícito | Claude (Lead), achado da Codex | Padding sozinho (`py-2.5` + `text-sm`) só alcançava ~40px, abaixo da faixa 44-52px do §21.1 |

## Ressalvas da Codex (QA) — pendências não-bloqueantes para a Fase 2+

Registradas na revisão de fechamento da Fase 1 (aprovado com ressalvas):

1. Considerar registrar radius/shadow/tipografia V2 diretamente nos namespaces do `@theme` (mantendo o sufixo `v2`) à medida que cada primitive migrar, permitindo `rounded-v2-lg`, `shadow-v2-md`, `text-v2-heading-xl` como utilities de verdade em vez de custom properties soltas.
2. Tokens semânticos (`--primary`, `--border` etc.) hoje repetem o hex das primitivas (`--interactive-600`, `--neutral-200`) em vez de referenciá-las via `var(...)`. Encadear (`--primary: var(--interactive-600)`) reduziria risco de divergência futura.
3. ~~`--text-placeholder: #98A2B3` sobre fundo branco tem contraste ~2,58:1~~ — **resolvido na Fase 2**: quando Input/Select passaram a consumir o token de fato, a Codex escalou isso a bloqueador (viola WCAG AA, §27.1). `--text-placeholder` passou a usar o valor de `--text-secondary`/`neutral-500` (`#667085`, ~4,98:1), um desvio deliberado do valor literal do documento-fonte (que mapeia placeholder para `neutral-400`) — `--text-disabled` manteve `#98A2B3` porque WCAG isenta componentes desabilitados do requisito de contraste.
4. O contrato Dialog+Select (`src/lib/ui/base-ui-select-dialog.ts`) segue estruturalmente intacto (nada da Fase 1 tocou nisso) — **verificado e promovido a teste permanente na Fase 2**: `src/lib/ui/base-ui-select-dialog.test.tsx` (baseado no smoke test que a Codex escreveu para a revisão, adaptado ao `vitest.config.ts` do repo). Passa: selecionar uma opção do Select não fecha o Dialog.

## Ressalvas da Codex (QA) — pendências não-bloqueantes para o restante da Fase 2

Registradas na revisão de fechamento do lote 1 (Button/Input/Select+CrmSelect/Badge/Card/Dialog+AlertDialog — aprovado com ressalvas):

1. `src/components/ui/select.tsx`: o `SelectContent` de base tinha `align="center"` como default, enquanto §18.1 pede alinhamento inicial (`align="start"`) — **corrigido**. `CrmSelectContent` já fixava `start` explicitamente, então nenhum consumidor via `CrmSelect` foi afetado; só o default do primitive cru mudou.
2. Dialog/AlertDialog ainda carregam dívida visual anterior à Fase 2: sem contrato estrutural de largura/margem no mobile nem de `max-height`/scroll interno quando o conteúdo é maior que a viewport. Não é regressão desta fase — registrar para tratar quando Dialog for revisitado (Fase 3+, ou quando um consumidor concreto expuser o problema).

## Decisões e achados — Fase 3, lote 1 (AppShell / CrmPageHeader / CrmSurfaceHeader)

| Data | Decisão/achado | Responsável | Motivo |
|---|---|---|---|
| 14/09/2026 | Sidebar: 88px→72px recolhida, 282px→248px expandida (§14.2); padding-left do conteúdo principal acompanhou | Claude (Lead) | Valor exato do documento-fonte |
| 14/09/2026 | Item ativo do nav: removido o "quadrado navy" ao redor do ícone e a sombra; agora `bg-interactive-50` + texto/ícone `interactive-700`, com barra lateral de 2px opcional (`before:`) | Claude (Lead) | §14.2 proíbe explicitamente ambos ("sem quadrado navy ao redor do ícone", "sem sombra") |
| 14/09/2026 | z-index do AppShell (mobile header/overlay/drawer, sidebar hover-overlay): números crus (40/50/60) trocados pelas custom properties `z-(--z-navigation)` etc. — **mesmos valores numéricos**, sem mudança visual, só formaliza a referência ao token definido na Fase 1 | Claude (Lead) | Entrega o que ficou pendente desde a Fase 1 ("migração de z-index é lote único coordenado na Fase 3") |
| 14/09/2026 | `CrmPageHeader`: removido o wrapper de card (`rounded-[28px] border shadow bg-white/70`), ícone deixou de ficar numa caixa 44×44 (agora solto, 20px), título perdeu o `md:` que duplicava o tamanho, badges/stats perderam sombra e foram para tokens | Claude (Lead) | §14.4 usa este componente como exemplo textual direto: "PageHeader deixa de ser um card gigante" |
| 14/09/2026 | `overflow-hidden` removido do `CrmPageHeader` (achado da Codex) | Claude (Lead), achado da Codex | Sobrou do card antigo (clipava cantos arredondados); sem o card, cortava o ring de foco dos botões de `actions` nas bordas |
| 14/09/2026 | `crmSurfaceCardClass`/`crmSurfaceHeaderClass`/`crmSurfaceSegmentedRoot/TabClass` (exports do `crm-surface-header.tsx`, usados no shell/toolbar do kanban): zinc-* trocado por tokens, raio para 12px (card) / 10px+8px (segmented), sombra removida do card | Claude (Lead) | §15.1 (Surface) e §20.1 (Tabs segmented) |
| 14/09/2026 | Verificação visual em browser **não realizada** neste lote — precisaria de sessão autenticada (Supabase) sem credenciais de teste disponíveis. Validação ficou restrita a lint/typecheck/vitest/build + revisão de diff e CSS compilado pela Codex | Claude (Lead) | Transparência: `DESIGN_SYSTEM_V2_CRM_BP.md` §36.4 pede inspeção visual quando há ambiente disponível — registrar a lacuna em vez de omitir |

Pendências do lote 1 — **resolvidas no lote 2**:

1. ~~Header mobile do AppShell mede ~64px; §14.3 pede 56-60px.~~ Corrigido: `py-3`→`py-2.5` (botão de 40px + 20px de padding = 60px, limite superior da faixa).
2. `#f8f9fb`/`#e6e9ef`/`#e1e5eb`/`#f3f5f8` continuam no `app-shell.tsx` (gradiente do logo BP não é considerado violação — é a marca, não uma ação/botão) — ainda fora de escopo; `sidebar-account-menu.tsx` e `crm-notifications-bell.tsx` (consumidos pelo AppShell) foram totalmente tokenizados no lote 2.

## Nota de metodologia — verificação de CSS compilado em rotas dinâmicas

Neste projeto (Next 16 + Turbopack), `.next/static/chunks/*.css` **não contém necessariamente o CSS de componentes usados só em rotas dinâmicas** (quase todas as rotas `/crm/*` são `ƒ Dynamic`, não `○ Static`) — o chunk pode ser gerado por rota, sob demanda. Grep nesses 3 arquivos deu falso-negativo para classes de `crm-notifications-bell.tsx` (ex.: a sombra composta com `var(--primary)`, o seletor `group/item`) mesmo após confirmar que a classe existe no código-fonte. Verificação definitiva: compilar a classe diretamente via `@tailwindcss/postcss` (script Node local, sem depender do bundler do Next) — assim confirma-se que o Tailwind gera CSS válido para a classe, independente de qual chunk do Next acaba recebendo essa regra. Usar esse método (não o grep nos chunks) quando uma classe usada só em componente de rota dinâmica precisar de confirmação.

## Ressalvas da Codex (QA) — pendências não-bloqueantes para um lote futuro da Fase 4 (Grok — Leads/Kanban)

Registradas no fechamento da Fase 4 (achado bloqueante já corrigido — ver Changelog):

1. Novo Lead: sem foco automático no primeiro campo inválido ao falhar a validação (§29.5). Dívida pré-existente à Fase 4, não regressão deste lote.
2. `new-demand-form.tsx`: algumas superfícies inline ainda com raio legado e sombra decorativa (linhas aproximadas 312, 1094, 1303) — destoa da convenção Surface (12px, sem sombra).
3. `pipeline-lead-card-content.tsx`, `pipeline-stage-panels.tsx`, `pipeline-kanban-panel-shell.tsx`: ainda usam cores Tailwind cruas para estado (ex. `amber-*`, `rose-*` diretos) em vez dos tokens semânticos `success`/`warning`/`danger`/`info` definidos na Fase 1.

## Bloqueios / pontos de atenção

| Data | Item | Status |
|---|---|---|
| 14/09/2026 | Terminal "Cursor — Frontend/UI" trava visualmente na tela "Trusting workspace..." e não respondeu a cliques/teclas em teste manual | Não resolvido — não bloqueia os demais agentes; verificar manualmente no Maestri |

## Equipe e responsabilidades (referência)

- **Claude — Arquiteto/Lead** (Maestro): coordena fases, decide ordem, resolve conflitos, aciona revisão do Codex
- **Codex — QA/Revisor**: revisão independente de cada entrega (build, lint, typecheck, regressão de contratos funcionais)
- **Cursor — Frontend/UI**: generalista de execução visual
- **Grok — Leads/Kanban**: migração visual do pipeline de leads e Kanban
- **Grok — Propostas/Contratos**: migração visual do builder de propostas e contratos (área sensível — conteúdo jurídico não pode ser reescrito)

## Decisões e achados — Fase 6, lote 1 (Clientes / Login / Perfil / Notificações)

| Data | Decisão/achado | Responsável | Motivo |
|---|---|---|---|
| 15/09/2026 | Login: removidos os blobs coloridos com blur, o gradiente de fundo (`bg-crm-gradient-dark`), o `glass-card`/translucidez e a caixa decorativa do logo com tinta teal; substituídos por fundo `bg-background` sólido, painel institucional opcional `bg-brand-navy` no desktop e card branco (`border-neutral-200`, `shadow-v2-sm`) para o formulário | Claude (Lead) | §29.1 do documento-fonte é explícito: "sem blobs coloridos, glass ou blur como linguagem principal", "logo sem gradiente decorativo ao redor", "formulário em superfície branca" |
| 15/09/2026 | Inputs do login/perfil: altura 48px (`h-12`) reduzida para 44px (`h-11`, "ação confortável excepcional" do cheat-sheet); overrides customizados de borda/fundo/sombra removidos para herdar o primitive `Input` V2 (já 40px/raio 8px por padrão, Fase 2) | Claude (Lead) | §29.1 pede "inputs de 40px ou 44px"; os overrides antigos (`border-primary-dark/10`, `bg-white/80`, `shadow-inner`) recriavam um estilo pré-V2 por cima do primitive já migrado |
| 15/09/2026 | `ProfileForm`: removido o `fieldClass`/`selectTriggerClass` customizados (recriavam raio/borda/sombra pré-V2 por cima de Input/Select já migrados na Fase 2); caixas de alerta (sem perfil, erro, sucesso) trocadas para os tokens semânticos `warning-*`/`destructive`/`success-*`; variantes `dark:` removidas (produto opera só em light mode, confirmado no Snapshot da Fase 0) | Claude (Lead) | Mesma lógica do login — parar de reimplementar estilo que os primitives já entregam |
| 15/09/2026 | `APP_USER_ROLE_LABELS` (`src/lib/crm/app-user-constants.ts`, cores cruas `bg-red-100 text-red-700` etc. dos badges de papel) **não tocado** neste lote | Claude (Lead) | É consumido também pela Administração → Utilizadores (Fase 6, lote de Administração, ainda não migrado); mexer agora dividiria a decisão de paleta categórica entre dois lotes. Registrar como pendência do lote de Administração |
| 15/09/2026 | `NotificationList`: `glass-card-no-float` e opacidades sobre `primary-dark` trocados por `Card` padrão (borda `neutral-200`, sem sombra) com destaque de não-lida via `border-interactive-300 bg-interactive-50`; ícones de status do Levantamento DUE (Check/AlertCircle/Clock, antes `emerald-600`/`rose-600`/`amber-600`) migrados para os tokens semânticos `success-text`/`danger-text`/`warning-text` | Claude (Lead) | Mesmo padrão de cores de estado que a Fase 1 definiu; ao contrário da ressalva equivalente da Fase 4 (`pipeline-lead-card-content.tsx` etc., adiada por escopo), aqui o custo era baixo o suficiente para corrigir no mesmo lote |
| 15/09/2026 | Revisão desta entrega feita só por mim (Lead): nenhum agente Cursor/Grok/Codex estava conectado no Maestri desta sessão. Validação: lint (5 arquivos alterados, limpo), `npm run build` (compilou, todas as rotas geradas incl. `/login`, `/crm/perfil`, `/crm/notifications`, `/crm/clientes`), `npm run test` (480/480). Nenhuma lógica de negócio tocada: `onSubmit`/queries Supabase/constants de área e papel intactos, só classes Tailwind | Claude (Lead) | Transparência sobre lacuna de revisão em par, como já registrado nos lotes da Fase 3 quando a Codex ficou indisponível |

## Decisões e achados — Fase 6, lote 2 (Dashboard)

| Data | Decisão/achado | Responsável | Motivo |
|---|---|---|---|
| 15/09/2026 | `KpiCards`: 4 cards independentes com borda/fundo tintados por KPI (teal/âmbar/azul/rosa, sem relação com significado do valor) trocados por uma única Surface (`border-neutral-200 bg-white`) dividida em colunas por `divide-x`/`divide-y`; label em `text-v2-body-sm-medium` (13px), valor em `text-v2-display-sm` (30px) | Claude (Lead) | §29.2: "podem compartilhar uma única Surface dividida em colunas", "label em 13px", "valor em 30px", "não criar uma cor diferente para cada KPI sem necessidade" — a versão anterior violava as três regras (cor decorativa sem significado de variação) |
| 15/09/2026 | `DashboardEtapaDistribution` e `IndicatorApprovalQueue`: linhas com `shadow-sm`/`hover:-translate-y-0.5`/`hover:shadow-xl` (elevação decorativa) removidas; badge de contagem por etapa (antes pill teal) para `bg-interactive-100 text-interactive-700` | Claude (Lead) | §Movimento do cheat-sheet: "Botões e cards não sobem no hover"; contagem por etapa não é um estado semântico (sucesso/erro), então usa o token neutro-azul de interação, não uma cor de marca solta |
| 15/09/2026 | Validação: lint limpo, `npm run build` compilou todas as rotas, `npm run test` 480/480. Sem Cursor/Grok/Codex conectados — revisão só pelo Lead | Claude (Lead) | Mesma ressalva de transparência do lote 1 |

## Decisões e achados — Fase 6, lote 3 (Documentos / Due diligence)

| Data | Decisão/achado | Responsável | Motivo |
|---|---|---|---|
| 15/09/2026 | `PHASE_META`/`PunctualityBadge`/`PUNCTUALITY_FILTER_OPTIONS` (`due-diligence-panel.tsx`) e os estados de `AreaRow`/`PhaseStep` (`due-timeline-ui.tsx`): cores de fase/estado antes cruas (`sky-*`/`violet-*`/`amber-*`/`accent-teal`/`accent-green`) migradas para os 4 tokens semânticos de estado definidos na Fase 1 — `info-*` para "em curso/levantamento/ativo", `violet-*` para "compilação" (única fase sem estado semântico natural — usa a entrada "Violet" da tabela de estados do cheat-sheet, pensada exatamente para esse tipo de caso categórico), `warning-*` para "revisão/ajustes pendentes", `success-*` para "finalizada/entregue no prazo/área concluída" | Claude (Lead) | §29.10: "estados de prazo usam cor + label" — a versão anterior já seguia esse princípio, mas com uma paleta solta em vez dos tokens da Fase 1; unificar evita 6+ variações de azul/verde/âmbar coexistindo sem relação entre si |
| 15/09/2026 | `LeadCard` (artigo principal): sombra composta customizada (`shadow-[0_1px_2px_...,0_14px_34px_...]`) e elevação no hover (`hover:-translate-y-px hover:shadow-[...]`) removidas — card fica só com borda (`border-neutral-200`, ou `border-destructive/30` quando em atraso), sem sombra | Claude (Lead) | Cheat-sheet: "Card comum: sem sombra"; "Botões e cards não sobem no hover" — violação dupla do mesmo padrão já corrigido nos lotes 1 e 2 |
| 15/09/2026 | Estados "filled" (chip/pill ativo em `QuickFilterChip` e `PhasePill`, antes `bg-primary-dark`): migrados para `bg-brand-navy` — é o token V2 que substitui `--primary-dark` como "navy sólido preenchido", já em uso desde o lote 1 (login) | Claude (Lead) | `--primary-dark` é um token legado (Fase 1 registra que só migra nas Fases 3/6); `--brand-navy` é o equivalente direto no vocabulário V2 |
| 15/09/2026 | `DueDocumentRow.iconStyle` (ícone PDF em `rose-*`, PPT em `orange-*`) **não tocado** — deliberado | Claude (Lead) | Cor de tipo de arquivo é convenção universal de ícone (PDF vermelho, PPT laranja), não um estado do produto; mapear para tokens semânticos (`danger`/`warning`) estaria semanticamente errado (PDF não é "perigo", PPT não é "aviso") |
| 15/09/2026 | Todos os `rounded-xl`/`rounded-lg`/`rounded-2xl`/`rounded-[18px]` (mais de 30 ocorrências nos dois arquivos) trocados pela escala V2 por papel semântico, não pelo valor em pixel mais próximo: cards/painéis → `--radius-v2-xl` (12px), linhas/itens/inputs → `--radius-v2-md` (8px), botões/controles/chips de ícone → `--radius-v2-lg` (10px) | Claude (Lead) | `18px` é explicitamente proibido pelo cheat-sheet; mapear por papel (não só pelo valor antigo) evita, por exemplo, que uma linha de lista e um card de página acabem com o mesmo raio só porque os dois usavam `rounded-xl` antes |
| 15/09/2026 | Validação: lint limpo (3 arquivos), `npm run build` compilou todas as rotas incl. `/crm/documentos`, `npm run test` 480/480. Sem Cursor/Grok/Codex conectados — revisão só pelo Lead | Claude (Lead) | Mesma ressalva de transparência dos lotes 1 e 2 |

## Decisões e achados — Fase 6, Administração lote A (Campos/Cláusulas)

| Data | Decisão/achado | Responsável | Motivo |
|---|---|---|---|
| 15/09/2026 | `TYPE_BADGE_COLOR` (`field-config-panel.tsx`): mapa de 13 cores cruas do Tailwind (uma por tipo de campo — texto/e-mail/telefone/url/número/percentual/data/hora/utilizador/seleção/múltipla escolha/texto longo) removido; badge de tipo passou a ser um único estilo neutro (`bg-neutral-100 text-neutral-700`) | Claude (Lead) | O rótulo do tipo já aparece como texto no badge — a cor por tipo era só decorativa, não carregava significado de estado. Mesmo princípio já aplicado nos KPIs do lote 2 ("não criar uma cor diferente sem necessidade"); 13 hues arbitrários não mapeiam para nenhum conceito do cheat-sheet (nem estado semântico, nem paleta categórica de 6 cores dos gráficos) |
| 15/09/2026 | `clause-templates-admin-panel.tsx`: dezenas de ocorrências de `primary-dark` em opacidades variadas (`/5`, `/8`, `/10`, `/15`, `/20`, `/40`, `/50`, `/60`, `/70`, `[0.015]`, `[0.02]`, `[0.03]`) usadas como "tinta" genérica para texto/borda/fundo em toda a árvore Área → Tipo → Subtipo — todas convergidas para a escala neutra V2 (`text-foreground`/`text-muted-foreground`/`border-neutral-200`/`bg-neutral-50`/`bg-neutral-100`), sem tentar preservar a opacidade exata original | Claude (Lead) | Preservar 10+ variações de opacidade do mesmo token legado não tem valor — a escala neutra V2 (`neutral-50`…`neutral-700` + `foreground`/`muted-foreground`) já cobre a mesma gama de contraste com nomes com significado, igual ao padrão já usado nos lotes 1-3 |
| 15/09/2026 | `statusBadgeClass` (aprovada/revisão/descontinuada) e o badge "Obrigatória": mapeados para `success`/`warning`/`neutral` e `info` respectivamente; toggle "Ativa/Inativa" da cláusula (botão circular de check) migrado de `emerald-*`/`rose-*` cru para `success-*`/`danger-*` | Claude (Lead) | São estados reais do domínio (status de revisão jurídica, obrigatoriedade, ativo/inativo) — mapeamento direto para os 4 tokens semânticos da Fase 1, mesmo padrão dos lotes 2 e 3 |
| 15/09/2026 | Botões "Excluir" (AlertDialogAction) em `field-config-panel.tsx` e `clause-templates-admin-panel.tsx`: `bg-red-600`/`bg-rose-600` cru trocado por `bg-destructive` (token shadcn já tokenizado na Fase 1, mesmo usado nas mensagens de erro de formulário desde o lote 1) | Claude (Lead) | Ação destrutiva já tinha um token pronto — não precisa de cor solta |
| 15/09/2026 | Botões "Novo Campo"/"Criar Campo" (`field-config-panel.tsx`): removido o `bg-crm-gradient-primary text-white shadow` (gradiente decorativo legado) — passam a herdar o estilo padrão do `Button` primitive (variante primária azul sólida já migrada na Fase 2) | Claude (Lead) | Mesmo padrão dos lotes anteriores: parar de reimplementar por cima de um primitive que já está correto |
| 15/09/2026 | Caixas internas do diálogo "Novo Campo"/"Nova cláusula" (`border-white/50 bg-white/65`, translúcidas) trocadas por `border-neutral-200 bg-neutral-50` sólido; inputs/selects dentro delas perderam os overrides `bg-white/70`/`border-primary-dark/15` para herdar o primitive | Claude (Lead) | Translucidez é o mesmo padrão "glass" já banido nos lotes 1-3; os primitives já entregam borda/fundo corretos sozinhos |
| 15/09/2026 | `campos/page.tsx`: `TabsList` perdeu `shadow-sm` (decorativo); badges de contagem por funil (`text-primary-dark ring-1 ring-slate-200`) viraram `border border-neutral-200 text-foreground` | Claude (Lead) | Toolbar/tabs sem sombra por padrão (cheat-sheet) |
| 15/09/2026 | `APP_USER_ROLE_LABELS` (`src/lib/crm/app-user-constants.ts`) **não tocado** — não é importado por nenhum dos 4 arquivos deste lote, só por `ProfileForm` (já migrado no lote 1) e pelo painel de Usuários (próximo sub-lote de Administração) | Claude (Lead) | Confirma a pendência já registrada no lote 1: fica para o sub-lote de Usuários, quando os dois consumidores puderem ser revistos juntos |
| 15/09/2026 | Validação: lint limpo (4 arquivos), `npm run build` compilou todas as rotas incl. `/crm/admin/campos` e `/crm/admin/clausulas`, `npm run test` 480/480. Sem Cursor/Grok/Codex conectados — revisão só pelo Lead | Claude (Lead) | Mesma ressalva de transparência dos lotes anteriores |

## Decisões e achados — Fase 6, Administração lote B (Usuários)

| Data | Decisão/achado | Responsável | Motivo |
|---|---|---|---|
| 15/09/2026 | `APP_USER_ROLE_LABELS` (`src/lib/crm/app-user-constants.ts`), adiado desde o lote 1: `admin` (era `red-100/red-700`) → tokens `danger`, `comercial` (era `sky-100/sky-700`) → tokens `info`, `controladoria` (era `violet-100/violet-700`) → tokens `violet` (a entrada "Violet" do cheat-sheet — a única cor da paleta original que já tinha um token V2 dedicado com o mesmo nome), `financeiro` (era `emerald-100/emerald-700`) → tokens `success`. Chaves de role e `label` inalterados — só a string `color` mudou | Claude (Lead) | Mantém a mesma "família" de cor de cada role (vermelho/azul/violeta/verde), só trocando classes Tailwind cruas pelos tokens semânticos já definidos na Fase 1 — risco mínimo de estranhamento pra quem já reconhece essas cores, e os 4 badges de papel (que são estados reais de permissão, não decoração) passam a usar o mesmo vocabulário de `success`/`warning`/`danger`/`info` já aplicado nos lotes anteriores |
| 15/09/2026 | `userCapability()` (4 variantes: gestor de proposta, comercial geral, área interna, acesso padrão): `emerald-*`/`sky-*`/`slate-*` crus trocados por `success-*`/`info-*`/neutro (`border-neutral-200 bg-neutral-50 text-foreground`) | Claude (Lead) | Mesma lógica da role: são estados reais de capacidade operacional, não decoração — mapeamento direto pros tokens semânticos |
| 15/09/2026 | `AREA_META` (10 cores cruas do Tailwind, uma por área de prática — Socio/Cível/Trabalhista/etc.) **não tocado**, exceto a entrada `Outro` (era `slate-100/slate-700`, virou `bg-neutral-100`/`text-muted-foreground`) | Claude (Lead) | Ao contrário do `TYPE_BADGE_COLOR` do lote A (13 cores por tipo de campo, sem função — removido), aqui a cor tem função real: deixar o usuário escanear visualmente 10 áreas de prática distintas numa lista longa. Não existe um conjunto de 10 tokens semânticos ou categóricos no cheat-sheet pra isso (a paleta categórica de gráficos só tem 6 cores) — manter os Tailwind crus é a opção mais honesta. `Outro` é diferente: é o fallback "nenhuma área específica", então faz sentido semântico ser neutro em vez de ter uma cor de categoria arbitrária |
| 15/09/2026 | `UserFormDialog`: `DialogContent` parou de reimplementar raio/borda/fundo/sombra (`rounded-[28px] border-[#dfe5ee] bg-[#f8fafc] shadow-[0_36px_100px_...]`) por cima do primitive `Dialog` já migrado na Fase 2 — herda `rounded-(--radius-v2-2xl)`/`border-border`/`bg-white`/`shadow-v2-lg` direto. `DialogHeader` perdeu o gradiente decorativo (`linear-gradient(135deg,#ffffff...#eef5f3)`) e o blur circle (`blur-3xl` com `#d8bf82`) — texto/ícone ficam sobre fundo branco sólido com borda inferior | Claude (Lead) | Mesmo padrão "glass/gradiente banido como linguagem principal" já aplicado no login (lote 1) e nos diálogos do lote A — e o Dialog primitive já faz isso sozinho, reimplementar por cima é dívida, não estilo |
| 15/09/2026 | Botão "Excluir usuário" (`AlertDialogAction`): `bg-red-600 hover:bg-red-700` cru trocado por `bg-danger-text hover:bg-danger-text/90` — diferente do lote A (que usou `bg-destructive`, o token shadcn), aqui usei `danger-text` porque o botão precisa de um vermelho **sólido** de alto contraste (confirmação de exclusão), e a variante `destructive` do `Button` primitive é um tom "tinta" (`bg-destructive/10 text-destructive`), não sólida | Claude (Lead) | `--danger-text: #b42318` já é o vermelho de estado "perigo" da Fase 1 — reaproveitar em vez de inventar um vermelho novo, mas sem forçar a variante `outline`/tinta do Button, que ficaria fraca demais pra uma confirmação destrutiva |
| 15/09/2026 | Resto do arquivo (header do painel, grade de métricas, barra de filtros, lista/linhas, badges "Sem acesso"/"Revisar acesso"): `primary-dark`/`slate-*` crus convergidos pra `foreground`/`muted-foreground`/`neutral-*`; `rounded-[24px]`/`rounded-[20px]`/`rounded-xl` soltos pra `--radius-v2-xl`/`--radius-v2-lg`; `shadow-sm` decorativo removido dos cards/seções | Claude (Lead) | Mesmo padrão de convergência pra escala neutra V2 já usado nos 4 lotes anteriores |
| 15/09/2026 | Validação: lint limpo (3 arquivos), `npm run build` compilou todas as rotas incl. `/crm/admin/usuarios` e `/crm/perfil` (consome o mesmo `APP_USER_ROLE_LABELS`), `npm run test` 480/480. Sem Cursor/Grok/Codex conectados — revisão só pelo Lead. Build precisou de duas tentativas (outro lote de Administração rodando `next build` em paralelo na mesma sessão) | Claude (Lead) | Mesma ressalva de transparência dos lotes anteriores |

## Decisões e achados — Fase 6, Administração lote D1 (Integrações — RD Station/SharePoint/E-mail/WhatsApp)

| Data | Decisão/achado | Responsável | Motivo |
|---|---|---|---|
| 15/09/2026 | `SharePointConfigPanel` e `WhatsappDueConfigPanel`: badges/indicadores de status ("Configurado"/"Incompleto", variável de ambiente definida/faltando, destino ativo/inativo) trocados de `emerald-*`/`amber-*`/`slate-*` crus para os tokens semânticos `success-*`/`warning-*`; dezenas de caixas `rounded-xl`/`rounded-2xl border-[#e5e7eb]/[#dfe5ee]/[#e6ebf2] bg-white/50-70` convergidas pra `rounded-(--radius-v2-xl) border-neutral-200 bg-white` (ou `bg-neutral-50` quando o objetivo era diferenciar de um card branco ao lado); texto `text-[#102033]`/`text-slate-500` convergido pra `text-foreground`/`text-muted-foreground` | Claude (Lead) | Mesmo padrão de "estado real → token semântico, decoração → neutro" dos lotes A/B; essas duas telas são as que mais tinham indicadores de conexão/config (faz sentido, são configuração de integração externa) |
| 15/09/2026 | Ícones de cabeçalho decorativos (SharePoint `ShieldCheck`, WhatsApp `Wifi`, E-mail `Mail`) — antes `bg-blue-50 text-blue-600` / `bg-emerald-50 text-emerald-600` sem relação com estado — convergidos pra `bg-interactive-50 text-interactive-600` (o token de marca/interação, não um estado semântico, já que são só o ícone identificador do painel, não um indicador de saúde da conexão) | Claude (Lead) | Mesma lógica do §29.2 já aplicada no Dashboard (lote 2): cor só quando tem significado. Um ícone de cabeçalho de painel não é status, é identidade visual — cabe no token de marca, não no de sucesso |
| 15/09/2026 | `glass-card glass-card-no-float` removido de `RdSyncAdminPanel`, `SharePointConfigPanel` e `WhatsappDueConfigPanel` (3 `Card` raiz) — viram `Card` padrão do primitive (Fase 2), sem glass/translucidez | Claude (Lead) | Mesmo padrão "glass banido como linguagem principal" de todos os lotes anteriores |
| 15/09/2026 | Botão "Salvar destino" do modal do WhatsApp: `bg-crm-gradient-primary text-white shadow` (gradiente teal/verde legado) removido — volta a herdar o estilo padrão do `Button` primitive (`primary`, azul sólido) | Claude (Lead) | Mesmo achado sistêmico dos lotes anteriores: botões reimplementando decoração por cima do primitive já migrado |
| 15/09/2026 | `IntegracoesAdminTabs`: `TabsList` tinha um override `bg-muted/50 p-1.5` (translúcido, padding maior que o padrão) e cada `TabsTrigger` carregava um seletor `data-[state=active]:shadow-sm` — esse seletor não corresponde ao atributo real que o primitive Base UI expõe (`data-active`, não `data-state="active"`), então era CSS morto; o primitive já aplica sombra no item ativo via `data-active` internamente. Ambos removidos | Claude (Lead) | Achado análogo ao bug de `tailwind-merge`/bracket-syntax da Fase 2 (Codex): um override do consumidor que nunca chegava a ter efeito, mascarado porque o resultado visual "parecia certo" mesmo sem ele — o `TabsList`/`TabsTrigger` primitive da Fase 2 já cobre isso sozinho |
| 15/09/2026 | `LeadEmailConfigPanel` já estava com a maior parte do arquivo relativamente limpa (usa `Card`/`Alert`/`Tabs` sem reimplementar) — só badge de status do Outlook (`green-*`/`red-*`→`success`/`danger`), ícone de cabeçalho (`blue-*`→`interactive`), duas mensagens de sucesso (`text-green-600`→`text-success-text`) e alguns containers `rounded-lg`/`rounded-md border bg-card`/`bg-muted` convergidos pra `rounded-(--radius-v2-xl/md)` com `border-neutral-200`/`bg-white`/`bg-neutral-50` explícitos | Claude (Lead) | Menor volume de mudança porque o arquivo já vinha mais próximo do padrão V2 do que os outros 3 painéis |
| 15/09/2026 | D4Sign (`d4sign-*.tsx`, ~2.300 linhas) deliberadamente **fora de escopo** deste lote — vira o lote D2 de Administração | Claude (Lead) | Escopo já dividido assim na largada para manter os lotes em tamanho administrável |
| 15/09/2026 | Validação: lint limpo (6 arquivos), `npm run build` compilou todas as rotas incl. `/crm/admin/integracoes`, `npm run test` 480/480. Sem Cursor/Grok/Codex conectados — revisão só pelo Lead | Claude (Lead) | Mesma ressalva de transparência dos lotes anteriores |

## Decisões e achados — Fase 6, Administração lote C1 (Proposta-escopo/Catálogo)

| Data | Decisão/achado | Responsável | Motivo |
|---|---|---|---|
| 15/09/2026 | `proposal-catalog-admin-panel.tsx` (1014 linhas) confirmado **código morto** — não é importado por nenhuma rota; `admin/proposta-escopo/page.tsx` renderiza `ScopeCatalogShell` (`scope-catalog/`), não este componente. Migrado mesmo assim por estar explicitamente listado no escopo do lote e por já não custar nada tecnicamente (é um pass de classNames) — remover o arquivo seria decisão de limpeza de código, fora do que foi pedido para este lote visual | Claude (Lead) | Evitar scope creep (deletar arquivo não é "migração visual"); registrar o achado para a Fase 7 (limpeza) considerar remover de vez |
| 15/09/2026 | `scope-editor.tsx`: painel de preview ao vivo usava paleta "papel" própria (`from-[#f9f7f2] to-[#ede9dd]`, acentos `#24615b`) sem relação com nenhum token V2 nem com `CONTRACT_BODY_STYLE` (a paleta real do documento gerado, intocada desde a Fase 5) — era decoração legada, não um contrato de renderização de documento. Convergido para `bg-neutral-50`/`border-neutral-200`/`text-muted-foreground` como qualquer outro painel secundário | Claude (Lead) | Diferente do caso da Fase 5 (fonte/margens do PDF/DOCX real, mantidas de propósito): aqui não há vínculo com geração de documento — é só o preview do catálogo dentro do admin, mesmas regras de qualquer outra superfície |
| 15/09/2026 | Estados de placeholder (detectado/declarado/órfão) e de item (ativo/inativo, com/sem alterações) mapeados para `success`/`warning`/`danger` consistentemente entre `scope-editor.tsx`, `catalog-type-editor.tsx` e `catalog-delete-button.tsx` | Claude (Lead) | Mesmo padrão "estado real → token semântico" dos demais lotes de Administração |
| 15/09/2026 | `getPracticeAreaColors`/`getAreaLucideIcon` (`src/lib/crm/area-lucide-icon.ts`, usados em `scope-tree.tsx` para colorir chips de área de prática) **não tocados** — mesma categoria de achado do `AREA_META` do lote B: categorização funcional real (uma cor por área jurídica) sem token equivalente no cheat-sheet | Claude (Lead) | Consistência com a decisão já registrada no lote B; é `src/lib/`, fora da zona editável sem necessidade explícita |
| 15/09/2026 | `NewItemDialog`: removido o override `className="z-[130]" overlayClassName="z-[120]"` do `DialogContent` — o comentário do próprio primitive (`dialog.tsx`) explica que esses valores servem só para diálogos **aninhados** (filho por cima de outro Dialog já aberto), e este diálogo é sempre o único aberto (chamado direto do `ScopeCatalogShell`, nunca de dentro de outro Dialog) — o z-index default do primitive (100, = token `--z-dialog-floating`) já é correto | Claude (Lead) | Achado de CSS supérfluo/potencialmente incorreto copiado de outro consumidor, análogo ao `data-[state=active]` morto encontrado no lote D1 |
| 15/09/2026 | Validação: lint limpo (11 arquivos: `proposal-catalog-admin-panel.tsx` + 9 arquivos de `scope-catalog/` + a página; 1 warning pré-existente não relacionado em `admin/proposta-escopo/page.tsx`, arquivo não tocado por este lote), `npm run build` compilou todas as rotas incl. `/crm/admin/proposta-escopo`, `npm run test` 480/480. Sem Cursor/Grok/Codex conectados — revisão só pelo Lead. `scope-import/` (import wizard, lote C2) e D4Sign (lote D2) ficam para lotes futuros, possivelmente concorrentes com este | Claude (Lead) | Mesma ressalva de transparência dos lotes anteriores |

## Decisões e achados — Fase 6, Administração lote C2 (Proposta-escopo/Importação)

| Data | Decisão/achado | Responsável | Motivo |
|---|---|---|---|
| 15/09/2026 | `admin/proposta-escopo/importacao/page.tsx` já estava limpo (usa `CrmPageHeader`/`buttonVariants` V2) — nenhuma mudança necessária | Claude (Lead) | Já migrado indiretamente quando `CrmPageHeader`/`Button` foram tokenizados nas Fases 2/3 |
| 15/09/2026 | `ScopeImportShell`: caixa de erro `rose-*` → `danger-*`; seção "Lotes existentes" (`rounded-[24px] border-white/55 bg-white/72 shadow-sm`, um padrão "glass" solto que não usava a classe `glass-card` mas tinha o mesmo efeito translúcido) convergida pra `rounded-(--radius-v2-xl) border-neutral-200 bg-white` sem sombra; itens de lote (`bg-slate-50/80 hover:bg-slate-100`) pra `bg-neutral-50 hover:bg-neutral-100` | Claude (Lead) | Mesmo padrão "sem glass/translucidez como linguagem principal" de todos os lotes; a caixa `border-white/55 bg-white/72` é o mesmo efeito visual do `glass-card` só que escrito à mão, então contava como a mesma violação |
| 15/09/2026 | `ImportUploadPanel`/`ImportProgressPanel`/`SuggestionReviewList`: mesmo padrão de card `rounded-[24px] border-white/55 bg-white/72 shadow-sm` repetido nos 3 (upload, progresso, revisão) — convergido igual em todos pra manter os 3 passos do wizard visualmente consistentes entre si; `variant="teal"` nos botões primários trocado por `variant="primary"` (mesmo resultado renderizado, mas usa o nome de variante atual em vez do legado mantido só por compatibilidade, já que este código está sendo tocado agora de qualquer forma) | Claude (Lead) | Consistência entre os 3 passos do wizard; `teal`/`primary` renderizam idêntico desde a Fase 2, então não é mudança visual, só remove um nome legado onde o arquivo já estava sendo editado |
| 15/09/2026 | `SuggestionCard`: preview "papel" com o mesmo hardcode (`from-[#f9f7f2] to-[#ede9dd]`, texto `#24615b`) já encontrado e resolvido em `scope-editor.tsx` no lote C1 — mesma decisão aplicada aqui (convergido pra `bg-neutral-50`/`border-neutral-200`/`text-muted-foreground`, sem vínculo com `CONTRACT_BODY_STYLE` ou geração real de documento) | Claude (Lead) | Mesmo achado do lote C1, reaparecendo porque `SuggestionCard` e `ScopeSubtypeEditor` (de `scope-editor.tsx`) implementam o mesmo preview de template de forma independente/duplicada — consistência entre os dois |
| 15/09/2026 | Estados de sugestão (pendente/aprovado/rejeitado) e de documento (erro de upload/extração) mapeados pra `danger-text`/`warning-text` onde havia cor de status; badge de confiança (`Badge variant="secondary"`) mantido neutro — não é um estado de sucesso/erro, é uma métrica contínua | Claude (Lead) | Mesmo padrão "estado real → token semântico" dos demais lotes; confiança (%) não é bom nem ruim por si só, não cabe em `success`/`warning`/`danger` |
| 15/09/2026 | Validação: lint limpo (5 arquivos de `scope-import/`), `npm run build` compilou todas as rotas incl. `/crm/admin/proposta-escopo/importacao`, `npm run test` 480/480. Sem Cursor/Grok/Codex conectados — revisão só pelo Lead. `scope-catalog/` (lote C1) e D4Sign/Admin-Documentos (lotes ainda pendentes) intocados | Claude (Lead) | Mesma ressalva de transparência dos lotes anteriores |

## Decisões e achados — Fase 6, Administração lote D2 (D4Sign + Admin/Documentos) — fecha a Fase 6

| Data | Decisão/achado | Responsável | Motivo |
|---|---|---|---|
| 15/09/2026 | **Achado de segurança (fora do escopo puramente visual, corrigido no mesmo lote):** `d4sign-embed-dialog.tsx` aceitava qualquer `postMessage` com `data === "signed"`/`"wrong-data"` de **qualquer origem**, sem checar `event.origin`/`event.source` — uma página maliciosa aberta na mesma aba/iframe podia forjar a mensagem e disparar `onSigned` (que atualiza o lead como assinado) sem o signatário ter assinado nada de fato. Corrigido: `handleMessage` agora só aceita a mensagem quando `event.source === iframeRef.current?.contentWindow` **e** `event.origin === new URL(embedHost).origin`; também adicionado guard `signedHandledRef` para não disparar `onSigned` duas vezes na mesma sessão do diálogo (a D4Sign pode reenviar o `"signed"` no mesmo carregamento) | Claude (Lead) | Vulnerabilidade real de spoofing de evento de assinatura, encontrada ao revisar o arquivo para a migração visual do lote D2. Registrada aqui (e em `docs/system-context.md`) por afetar comportamento de segurança, não só classes Tailwind — excepciona a regra geral de "não tocar lógica de negócio durante trabalho visual" porque é a correção de uma falha, não uma reescrita de regra |
| 15/09/2026 | Teste novo permanente: `src/components/crm/d4sign-embed-dialog.test.tsx` — 3 casos (mensagem forjada de origem/fonte errada é ignorada; mensagem válida do iframe configurado é aceita; mensagens válidas repetidas só disparam `onSigned` uma vez) | Claude (Lead) | Cobertura para a correção de segurança, seguindo o padrão de testes permanentes já usado no contrato Dialog+Select (Fase 2) |
| 15/09/2026 | `d4sign-view-dialog.tsx`: `DialogContent` ganhou a prop `hideCloseButton` (já existente no primitive `Dialog`, Fase 2, só não usada aqui) — o header do próprio componente já tinha um botão "Fechar" custom; sem a prop, o primitive desenhava um segundo X de fechar sobreposto/redundante no canto | Claude (Lead) | Achado de UI duplicada, mesma categoria dos achados de CSS morto/redundante das lotes A/C1/D1 (ex.: `data-[state=active]` do D1) |
| 15/09/2026 | `admin/documentos/page.tsx`: condição do estado vazio corrigida de `templates.length === 0` para `!error && templates.length === 0` — sem a guarda, um erro ao buscar os templates (Supabase fora do ar, migration pendente) mostrava simultaneamente o banner de erro **e** o cartão "Nenhum modelo ativo encontrado", uma mensagem confusa e redundante | Claude (Lead) | Mesma categoria dos achados anteriores: bug pequeno e real encontrado ao tocar no arquivo para a migração visual, não scope creep — a tela ficava incoerente nesse caso de erro |
| 15/09/2026 | D4Sign (`d4sign-dashboard.tsx` ~700 linhas, `d4sign-embed-dialog.tsx`, `d4sign-health-panel.tsx`, `d4sign-link-lead-dialog.tsx`, `d4sign-view-dialog.tsx`): mesma convergência de padrão dos lotes anteriores — `glass`/translucidez removida, `slate-*`/`teal-*`/`emerald-*`/`amber-*`/`rose-*`/`violet-*` crus mapeados para os tokens semânticos (`success`/`warning`/`danger`/`info`/`violet`) e de marca (`interactive-*`, `brand-navy`) da Fase 1, raios `rounded-xl`/`rounded-lg`/`rounded-full`/`rounded-2xl` convergidos pra escala V2 por papel (card/painel → `--radius-v2-lg/xl`, linha/badge/pill → `--radius-v2-sm/md`, avatar/ícone circular → `--radius-v2-full`), sombras decorativas (`shadow-sm`) removidas dos cards estáticos | Claude (Lead) | Mesmo padrão sistemático de todos os lotes de Administração anteriores |
| 15/09/2026 | Validação: lint limpo (7 arquivos + 1 teste novo), `npm run build` compilou todas as rotas incl. `/crm/leads/[id]` (consome os componentes D4Sign) e `/crm/admin/documentos`, `npm run test` 483/483 (480 + 3 testes novos da correção de segurança). Sem Cursor/Grok/Codex conectados — validado só pelo Lead. Nenhuma lógica de envio/sincronização/webhook D4Sign alterada — só o handler de `postMessage` do EMBED (correção de segurança) e a guarda de estado vazio do Admin/Documentos (correção de bug), ambos registrados acima | Claude (Lead) | Mesma ressalva de transparência dos lotes anteriores; **Fase 6 encerrada com este lote** |
| 15/09/2026 | **Pendência de QA registrada**: pedido de revisão retroativa do lote D2 enviado ao Codex (agora conectado) após o commit/push — Codex bateu no limite de uso do ChatGPT ("try again at 4:17 PM") antes de devolver veredito formal. Indício não-conclusivo no scrollback dele (de uma passada anterior, não uma resposta formal a este pedido): "nenhuma alteração estrutural ou de regra de negócio entrou no escopo" para as 3 correções (idempotência do `signed`, botão de fechar duplicado, guarda `!error`). Decisão do usuário: não bloquear a Fase 7 por isso — reagendar o pedido ao Codex para depois das 16:17 | Claude (Lead), decisão do usuário | Primeira vez nesta sessão com Codex de fato conectado — o processo de revisão por lote (usado desde a Fase 1) deve valer também retroativamente, não só ser pulado quando "ninguém está conectado" |

## Decisões e achados — Fase 7 (Limpeza) — kickoff e divisão de escopo

| Data | Decisão/achado | Responsável | Motivo |
|---|---|---|---|
| 15/09/2026 | Auditoria de escopo antes de delegar: `.glass-card`/`.glass-card-no-float` restam em 2 arquivos (`lead-notes-tab.tsx`, `perfil/page.tsx`); Inter **já em zero ocorrências** (nada a fazer — já removida em lotes anteriores); `rounded-[Npx]` arbitrário resta em 6 arquivos; token legado `primary-dark` resta em 17 arquivos, `accent-teal` em 5 | Claude (Lead) | Fase 7 não é "faz uma limpeza geral" — precisa de lista concreta de arquivos pra dividir entre os agentes sem sobreposição nem arquivo esquecido |
| 15/09/2026 | Escopo dividido por área/dono, não por tipo de arquivo — evita dois agentes editando o mesmo arquivo: **Grok — Leads/Kanban** fica com os arquivos da área de Leads/Kanban (inclui as pendências já registradas nas Fases 4 e 6: `pipeline-lead-card-content.tsx`, `pipeline-stage-panels.tsx`, `new-demand-form.tsx` linhas ~312/1094/1303); **Grok — Propostas/Contratos** fica com a área de Propostas/Contratos; **Cursor — Frontend/UI** fica com os widgets compartilhados de uso geral (não pertencem a uma área de negócio específica) | Claude (Lead) | Mesmo padrão de ownership já usado nas Fases 4/5 (Grok por área de negócio sensível) — Fase 7 toca os mesmos arquivos, então segue a mesma divisão |
| 15/09/2026 | `globals.css` (remoção de aliases de token legados não mais consumidos por ninguém) **não delegado** — fica comigo (Lead) para depois que os 3 agentes terminarem, e não em paralelo | Claude (Lead) | É o único arquivo compartilhado por todas as áreas nesta fase; editar em paralelo por 3 agentes diferentes é o tipo de conflito de merge que a divisão por arquivo evita nos outros itens — mais seguro fazer essa parte depois, com visão completa do que cada área realmente parou de usar |
| 15/09/2026 | **Limpeza de `globals.css` concluída** depois que os 3 agentes reportaram: reauditei `primary-dark`, `primary-medium`, `primary-light`, `accent-teal`, `accent-pink`, `accent-green`, `crm-surface-warm(-muted)`, `crm-border-warm(-strong)`, `glass-white`, `glass-blur`, `glass-border`, `gradient-primary`/`.bg-crm-gradient-primary` e `gradient-dark`/`.bg-crm-gradient-dark` (incl. `.glass-card`/`.glass-card-no-float`, já com 0 consumidores antes disso) — todos com **zero consumidores** em `src/**` depois dos 3 lotes desta fase. Removidos do `@theme inline` e do `:root`, junto com as duas classes de gradiente e o bloco `.glass-card`. `accent-yellow`/`accent-yellow-dark` **mantidos** (único consumidor real: gradiente da logo BP em `app-shell.tsx`, decisão de marca já registrada na Fase 3) | Claude (Lead) | Fase 7 item 1 do documento-fonte ("remover aliases legados não usados") — só depois de ver os 3 lotes prontos dava pra confirmar com segurança que não sobrava nenhum consumidor |
| 15/09/2026 | **Achado próprio, corrigido no ato**: o comentário que escrevi pra substituir o antigo (explicando a remoção) continha a sequência `--glass-*/--crm-surface-warm-*`, cujo `*/` no meio do texto fechou o comentário CSS antes da hora — quebrou o build (`CssSyntaxError: Unknown word` na linha seguinte). Mesma classe de erro que a Codex encontrou na revisão da Fase 1 (comentário CSS malformado). Corrigido reescrevendo sem a sequência `*/` literal, build voltou a compilar limpo | Claude (Lead) | Lição já documentada desde a Fase 1 não impediu de eu repetir o mesmo tipo de erro — reforça por que rodar `npm run build` depois de qualquer edição em `globals.css` não é opcional |
| 15/09/2026 | Validação final combinada dos 22 arquivos dos 3 lotes + a limpeza do `globals.css`: lint 0 erros (14 warnings pré-existentes, nenhum novo), `npm run test` 483/483, `npm run build` compilou todas as rotas. Ainda sem revisão do Codex (limite de uso não resetou até agora) — **não commitado/pushado**, aguardando o Codex ou decisão do usuário de seguir sem ele | Claude (Lead) | Mesmo padrão de transparência de todos os lotes anteriores |
| 15/09/2026 | Tarefas despachadas via `maestri ask --batch` para os 3 agentes em paralelo; cada um deve reportar de volta via `maestri ask "Claude — Arquiteto/Lead" ...` ao terminar, para eu consolidar e (quando o Codex voltar do limite de uso, depois das 16:17) mandar para revisão antes de comitar | Claude (Lead) | Fluxo correto do time conforme `AGENTS.md`: Lead planeja e decide escopo, agentes executam, Codex revisa antes de fechar |
| 15/09/2026 | Os 3 agentes (Cursor/Grok-Leads/Grok-Propostas) ficaram travados em prompts de aprovação interativa do Cursor Agent CLI (um por arquivo escrito) — precisei mandar Shift-Tab (`maestri ask --raw "\e[Z"`) pra cada um ativar "Run Everything" e seguir sem travar em cada edição | Claude (Lead) | Achado operacional: delegar via `maestri ask --batch` não é fire-and-forget quando o agente roda em modo interativo por padrão — precisa desse passo extra de liberação, registrar para não repetir a surpresa em lotes futuros |
| 15/09/2026 | **Grok — Leads/Kanban concluiu e reportou** (sem commit/push): 10 arquivos (`lead-notes-tab.tsx`, `lead-lifecycle-timeline-panel.tsx`, `new-demand-form.tsx` — pendência da Fase 4 finalmente resolvida —, `pipeline-kanban-panel-shell.tsx`, `pipeline-lead-card-content.tsx`, `pipeline-stage-panels.tsx` — as 3 pendências de cor crua de estado da Fase 4 resolvidas —, `lead-add-empresa-button.tsx`, `lead-detail-field-editor.tsx`, `lead-intake-empresa-block.tsx`, `lead-d4sign-panel.tsx`). Além do briefing original, limpou no mesmo passe (mesmo padrão dos lotes anteriores — dívida encontrada no arquivo já sendo tocado): gradiente dourado/hex soltos em `lead-notes-tab.tsx`, `LEAD_TYPE_TONES` (5 paletas cruas) em `new-demand-form.tsx`, e um gradiente legado (`bg-crm-gradient-primary`) no chip ativo de multiselect em `lead-detail-field-editor.tsx`. Nenhum bug funcional ou CSS morto encontrado neste lote (confirmado explicitamente por quem executou). Lint 0 erros, 483/483 testes, build limpo incl. `/crm/leads` e `/crm/leads/[id]`. `globals.css` intocado | Grok — Leads/Kanban | Aguardando revisão do Codex |
| 15/09/2026 | **Cursor — Frontend/UI concluiu e reportou** (sem commit/push): 8 arquivos (`perfil/page.tsx`, `app-shell.tsx`, `crm-user-label.tsx`, `days-in-stage-panel.tsx`, `condition-builder.tsx`, `dynamic-form.tsx`, `sticky-note.tsx` — código morto, sem consumidores no repo — e o primitive `avatar.tsx`, mapeando todos os consumidores do fallback default antes de mudar a cor). `glass-card` removido do perfil; `primary-dark`/`accent-teal` convergidos para `foreground`/`brand-navy`/`interactive-*`; achado extra corrigido dentro do escopo: `rounded-[14px]` em `app-shell.tsx` (14px é valor explicitamente proibido pelo cheat-sheet) → `--radius-v2-md`; gradiente do logo BP no shell intocado (é marca, decisão já registrada na Fase 3). Lint 0 erros, 483/483 testes, build limpo incl. `/crm/perfil` | Cursor — Frontend/UI | Aguardando revisão do Codex |
| 15/09/2026 | **Auditoria de `rounded-full` em botões comuns** (pedida como tarefa extra ao Cursor, só levantamento, sem edição): 3 casos claros de botão comum (não pill) usando `rounded-full` indevidamente — `confeccao-reuniao-modal-section.tsx:368` (Button outline "Adicionar linha"), `confeccao-reuniao-modal-section.tsx:436` (Button secondary "Buscar CEP"), `contract-portfolio-tab.tsx:217` (ação de ícone "abrir contrato"); 3 casos borderline deixados para eu decidir: `sidebar-account-menu.tsx:58` (vira circular só quando a sidebar está recolhida, envolvendo um Avatar — circular faz sentido aí), `clause-templates-admin-panel.tsx:638` (toggle circular de 20px ativo/inativo, não é pill de texto) e `lead-notes-tab.tsx:632` (botão "X" de remover menção, círculo pequeno de ação — já corrigido dentro do próprio lote D2/D4Sign anterior o padrão equivalente). **Decisão do Lead**: os 2 primeiros (`confeccao-reuniao-modal-section.tsx`) e o de `contract-portfolio-tab.tsx` são o padrão real que a Fase 7 pede pra remover (botão de ação comum não deveria ser pill); os 3 borderline (`sidebar-account-menu`, `clause-templates-admin-panel`, `lead-notes-tab`) são círculos de propósito (ícone/avatar/toggle/X pequeno), não o "botão comum" que o documento-fonte tem em mente — mantidos como estão. Fica registrado como pendência de um micro-lote futuro (baixo risco, 3 arquivos, 1 classe cada) | Claude (Lead) | Julgamento de escopo: nem todo `rounded-full` é a violação que a Fase 7 quer corrigir — só botões de ação comuns virando pill sem motivo |
| 15/09/2026 | **Grok — Propostas/Contratos concluiu e reportou** (sem commit/push): 5 arquivos, +37/−37 (`simulacao/page.tsx`, `contrato-document-builder.tsx`, `contrato-object-panels.tsx`, `proposta-escopo-por-area.tsx`, `contract-signers-kanban-panel.tsx`) — `primary-dark` convergido para `brand-navy`/`foreground`/`muted-foreground`/tokens neutros, `accent-teal` para `interactive-600`, um `rounded-[10px]` para `--radius-v2-md`. CONTRACT_BODY_STYLE, geração DOCX/PDF, valores/allocations, cláusulas, pins e o contrato Dialog+Select confirmados intactos — nenhuma lógica tocada. Lint 0 erros/14 warnings pré-existentes, 483/483 testes, build limpo. **Achados extras registrados para lote futuro (deliberadamente não tocados, fora do briefing)**: `zinc-*`/`rounded-2xl`/`shadow-sm` crus em `simulacao/page.tsx`; `rose-*` no prazo atrasado e `emerald`/`blue`/`slate` crus no header D4Sign de `contrato-document-builder.tsx`; `amber`/`emerald`/`rose` crus em `contrato-object-panels.tsx`; empty state ainda "glass" (`border-white/40 bg-white/40`) em `proposta-escopo-por-area.tsx`; `emerald`/`teal`/`amber`/`sky` crus e gradiente decorativo remanescentes em `contract-signers-kanban-panel.tsx` | Grok — Propostas/Contratos | Aguardando revisão do Codex (fora do budget até 16:17) antes de eu coordenar o commit |

## Redesign do detalhe do lead — shell e Visão geral

| Data | Decisão/achado | Responsável | Motivo |
|---|---|---|---|
| 15/09/2026 | A ficha `/crm/leads/[id]` passou de uma Visão geral em coluna única, composta por mini-cards equivalentes, para um workspace responsivo em duas colunas: resumo e cadastro agrupado à esquerda; contexto, ações reais e atividade recente no rail direito. O cadastro agora separa solicitante, prazos/reunião, escopo/indicação, empresas e integração SharePoint. Erros longos do SharePoint ficam condensados num alerta com detalhes técnicos expansíveis, sem perder a edição existente. | Cursor | Combinar a hierarquia clara do mockup fornecido com a densidade útil do RD Station, sem reproduzir a parede de propriedades do CRM terceirizado. |
| 15/09/2026 | O shell ganhou uma faixa de destaques com etapa, autor do cadastro, última atualização e origem/tipo. O rótulo “Responsável” foi corrigido para “Cadastro realizado por”: o dado carregado nesta página vem de `lead_intakes.cadastrado_por_email`, não de um responsável comercial próprio. Pessoas continuam com `CrmUserLabel`; a variante compacta de `LeadDetailFieldEditor` evita cards aninhados e mantém a variante anterior como default nas outras tabs. | Cursor | Corrigir uma ambiguidade de informação, melhorar leitura por varredura e preservar o contrato visual V2 (`CrmEntityHeader` claro, tabs `line`, Surface sem sombra). |
| 15/09/2026 | Escopo funcional preservado: nenhuma query, API, migration ou regra de negócio alterada; condições, tab inicial, Realtime e builders de Due/Proposta/Contrato/Faturamento/Assinatura permanecem intactos. Adicionados 2 testes para o agrupamento dos campos do cadastro. Validação visual realizada no lead Louvetique em 1920px e 390px, incluindo navegação pelo atalho de Anotações e abertura/cancelamento da edição inline. | Cursor | Manter a alteração revisável e limitada ao shell + Visão geral escolhidos pelo usuário. |

## Correção fora de fase — header do detalhe do lead (feedback direto do usuário em dev)

| Data | Decisão/achado | Responsável | Motivo |
|---|---|---|---|
| 15/09/2026 | Usuário reportou em modo dev que o header do detalhe do lead (`/crm/leads/[id]`) estava visualmente poluído — "jogado com um monte de info abaixo" do nome. Diagnóstico (Lead): `LeadDetailHero` (`lead-detail-view.tsx`) empurrava até 7 itens condicionais pro `context` do `CrmEntityHeader` (Etapa, Tipo, Responsável, Atualização/Criação, DUE, Funil, Escopos), enquanto o §29.4 do documento-fonte especifica só 4 ("etapa, valor, responsável e última atualização") — o excesso de itens era a causa raiz, não um problema de CSS. Corrigido pelo Cursor: `context` reduzido a 3 (Etapa, Responsável, Atualização/Criação — "valor" não existe no modelo de dados deste lead, então não foi adicionado); "Tipo" movido pra `badges` (pill, ao lado do badge de funil); DUE/Funil/Escopos removidos do header (continuam disponíveis nas abas correspondentes); `pipelineStepLabel`/`stageIndex`/`pipelineColumns` e os imports `SALES_PIPELINE_COLUMNS`/`POS_VENDA_PIPELINE_COLUMNS` removidos por só servirem ao item "Funil" agora eliminado. `ddSimNao` e `proposalScopeSummary` (usados em outros pontos do arquivo) não foram apagados, só deixaram de ser renderizados neste header. Primitive `CrmEntityHeader` intocado — o problema era só de quantidade de itens, não do componente | Cursor — Frontend/UI, diagnóstico Claude (Lead) | Correção de arquitetura de informação, não estética: a spec já previa 4 itens, a implementação tinha quase o dobro |
| 15/09/2026 | Verificação visual feita de fato em navegador (sessão autenticada já aberta, `http://localhost:3000/crm/leads/[id]` do lead "Louvetique") — primeira vez nesta fase que isso foi possível, ao contrário da ressalva registrada em lotes anteriores por falta de credenciais de teste. Confirmado: breadcrumb → badges (Funil comercial + Indicação) → título/ações → Etapa/Responsável/Atualização, sem DUE/Funil N-M/Escopos | Cursor — Frontend/UI | Fecha a lacuna de verificação visual registrada desde a Fase 3 |
| 15/09/2026 | Validação: lint 0 erros (14 warnings pré-existentes, nenhum no arquivo), 483/483 testes, build limpo incl. `/crm/leads/[id]`. Sem commit/push — aguardando o usuário confirmar visualmente por conta própria (ele estava olhando em dev quando reportou o problema) antes de eu coordenar o commit junto com o resto da Fase 7 pendente | Claude (Lead) | Decisão de deixar a confirmação final para quem reportou o problema, já que ele tem a sessão de dev aberta |

## Changelog

- **18/09/2026** — `/crm/clientes` e grade pública: Status e Áreas deixam de depender só do fetch ao vivo do OrquestrAI (que, se falhar no online, renderizava “—”). Sync grava `gestor_atividade`, `responsible_area` e `legal_areas` em `grupos_economicos`; a UI usa o espelho local e só sobrepõe se o fetch cruzado responder. Áreas unem `responsible_area` ∪ `legal_areas` ∪ `areas_atuacao`. Tokens V2 intactos. Sem mudança de tokens globais.
- **18/09/2026** — `/crm/clientes` modal de grupo (editar): chips de **Áreas** (Cível/Trabalhista/etc.) passaram a ser o controle clicável (`type="button"`, hint “Manual, se marcada” dentro do botão). O Dialog passou a usar o contrato Dialog+Select (`modal={false}` + `dialogSelectOutsideHandlers` + `CrmSelectContent inModal`) para o Select de Indicação não engolir o clique. Sem mudança de tokens globais.
- **18/09/2026** — `/crm/clientes`: coluna **Categoria** passou a ser Cliente | Lead (`origemLinha`), não Cível/Societário. Grupos da carteira mostram Cliente; Lead fica no tipo da linha para unir `oportunidades` depois. Áreas continuam `responsible_area` ∪ SIOE. Sync grava `categoria='Cliente'`. Tokens V2 nos badges (`info`/`violet`). Sem mudança de tokens globais.
- **18/09/2026** — Preenchimento público da carteira: um único link `/preencher/carteira/[token]` (sem login) abre a **grade** de grupos, com edição inline (tipo/subtipo/quem indicou/áreas) e auto-save por linha. Na aba Clientes, um botão “Copiar link de preenchimento” (sem URL por linha). Tokens V2 na grade pública. Sem mudança de tokens globais.
- **18/09/2026** — `/crm/clientes` modal: botão Editar no header (admin/controladoria) grava Indicação e Áreas. Cível/`responsible_area` ficam em Áreas; Categoria mostra “—”. Tokens V2 no Button outline. Sem mudança de tokens globais.
- **18/09/2026** — `/crm/clientes`: clicar no grupo abre modal (identidade, status, categoria, indicação, áreas, empresas/CNPJs e pessoas). Categoria = `email_client_groups.responsible_area` (coluna na tabela + modal; persistida em `grupos_economicos.categoria` no sync). Tokens V2 no dialog (`radius-v2-2xl`, `shadow-v2-lg`). Sem mudança de tokens globais.
- **18/09/2026** — Hub `/crm/contratos`: removido o parágrafo descritivo sob o título; faixa de KPIs ganhou contratos por área, grupos ativos × contratos e alerta danger de Cliente ativo sem contrato (lista + atalho para `/crm/clientes`). Carteira compacta (1 linha, máx. 2) e filtros alinhados (`h-10` + rótulo Buscar). Tokens V2 nos cards/tabela tocados; sem glass/gradiente. Sem mudança de tokens globais.
- **18/09/2026** — `/crm/clientes`: ordenação por Grupo (A–Z) e Status (Cliente ativo → inativo → sem informação), headers clicáveis; colunas Indicação e Áreas; geração/cópia de links públicos `/preencher/carteira/[token]` (tokens V2, `AreaIconLabel`). Status continua `gestor_atividade`. Sem mudança de tokens globais.
- **18/09/2026** — `/crm/clientes`: coluna Status passou a exibir Cliente ativo/inativo a partir de `email_client_groups.gestor_atividade` (OrquestrAI). Títulos SIOE ficam nas colunas Abertos/Pagos/Valor aberto. Badges com tokens `success`/`neutral`. Sem mudança de tokens globais.
- **18/09/2026** — Hub `/crm/contratos` carteira: título da linha passou a ser o grupo econômico e o subtítulo a razão social (não o apelido OrquestrAI tipo "LOJA 01"). Sem mudança de tokens.
- **17/09/2026** — Carteira de grupos + importação de contratos PDF (funcional, tokens V2): `/crm/clientes` passou a listar grupos do OrquestrAI (`email_client_groups` / `email_companies` / `email_people`) com resumo de títulos SIOE; wizard `/crm/contratos/importacao` e atalho no hub de contratos. Sem mudança de tokens globais nem de regra de faturamento/ativação. DDL `20260917202943_carteira_grupos_contract_import.sql` versionada, **não aplicada no remoto**.
- **15/09/2026** — Fase 6, Administração lote D2 (D4Sign + Admin/Documentos) — **fecha a Fase 6**: `d4sign-dashboard.tsx`, `d4sign-embed-dialog.tsx`, `d4sign-health-panel.tsx`, `d4sign-link-lead-dialog.tsx`, `d4sign-view-dialog.tsx` e `admin/documentos/page.tsx` migrados. Dois achados fora do escopo puramente visual, corrigidos no mesmo lote por serem bugs reais (não scope creep): (1) vulnerabilidade de spoofing no `postMessage` do EMBED de assinatura — qualquer origem podia forjar `"signed"` e marcar um contrato como assinado sem o signatário assinar nada; corrigida com checagem de `event.origin`/`event.source` + guarda contra disparo duplo, com 3 testes novos permanentes; (2) `admin/documentos/page.tsx` mostrava banner de erro e "nenhum modelo encontrado" ao mesmo tempo quando a busca falhava — corrigido com guarda `!error`. `docs/system-context.md` atualizado na mesma alteração para registrar a validação de origem do EMBED. Sem Cursor/Grok/Codex conectados — validado só pelo Lead (lint limpo, build limpo incl. `/crm/leads/[id]` e `/crm/admin/documentos`, 483/483 testes — 480 + 3 novos). Nenhuma lógica de envio/sincronização/webhook D4Sign alterada.
- **15/09/2026** — Fase 6, Administração lote C2: Proposta-escopo/Importação (`admin/proposta-escopo/importacao/page.tsx` já limpo, e os 5 arquivos de `scope-import/`: shell, painel de upload, painel de progresso, lista de revisão, card de sugestão) migrados. Achados: 3 dos 5 arquivos tinham o mesmo card "glass sem a classe glass-card" (`border-white/55 bg-white/72 shadow-sm`) — mesma violação de linguagem visual das demais, convergido pra `border-neutral-200 bg-white`; o preview "papel" com paleta hardcoded (`#f9f7f2`/`#ede9dd`/`#24615b`) já visto em `scope-editor.tsx` no lote C1 reaparecia aqui em `SuggestionCard` (mesma solução aplicada). Sem Cursor/Grok/Codex conectados — validado só pelo Lead (lint/build/480 testes limpos). Nenhuma lógica alterada: upload/retry, polling de progresso, extração/consolidação por IA, aprovação/rejeição de sugestões — tudo intacto.
- **15/09/2026** — Fase 6, Administração lote C1: Proposta-escopo/Catálogo (`admin/proposta-escopo/page.tsx` sem alteração de conteúdo, `proposal-catalog-admin-panel.tsx` — código morto migrado mesmo assim, e os 9 arquivos de `scope-catalog/`: shell, árvore, editor de subtipo, editor de tipo, dialog de criação, botão de exclusão, barra de inserção de placeholder, campo de textarea com placeholders, estado vazio) migrados. Achado: o preview "papel" do editor de subtipo usava uma paleta hardcoded sem relação com a renderização real do documento (diferente do caso análogo da Fase 5) — convergido para tokens neutros. Removido um override de z-index de diálogo desnecessário (`NewItemDialog`, cabia no default do primitive). `getPracticeAreaColors`/`AREA_META`-like categorização de área mantida intencionalmente crua, mesma lógica do lote B. Sem Cursor/Grok/Codex conectados — validado só pelo Lead (lint/build/480 testes limpos). Nenhuma lógica alterada: CRUD do catálogo, árvore de seleção, merge/detecção de placeholders, preview ao vivo do texto — tudo intacto.

- **15/09/2026** — Fase 6, Administração lote D1: Integrações não-D4Sign (`admin/integracoes/page.tsx`, `integracoes-admin-tabs.tsx`, `rd-sync-admin-panel.tsx`, `sharepoint-config-panel.tsx`, `whatsapp-due-config-panel.tsx`, `lead-email-config-panel.tsx`) migrado. Indicadores de status de conexão/configuração (SharePoint "Configurado"/"Incompleto", variáveis de ambiente definidas/faltando, destino WhatsApp ativo/inativo, Outlook conectado) tokenizados pra `success`/`warning`/`danger`; `glass-card` removido de 3 painéis; botão com gradiente legado do modal do WhatsApp volta a herdar o `Button` primitive; achado de CSS morto no `IntegracoesAdminTabs` (`data-[state=active]` não corresponde ao atributo real `data-active` do primitive Base UI — removido, já coberto pelo primitive). D4Sign (~2.300 linhas) fica pro próximo sub-lote (D2). Sem Cursor/Grok/Codex conectados — validado só pelo Lead (lint/build/480 testes limpos). Nenhuma lógica alterada: sync RD Station, teste de conexão SharePoint, CRUD de destinos WhatsApp/Evolution API, templates e OAuth de e-mail de leads — tudo intacto.

- **15/09/2026** — Fase 6, Administração lote B: Usuários (`admin/usuarios/page.tsx`, `user-management-panel.tsx`) migrado — resolvida a pendência do lote 1, `APP_USER_ROLE_LABELS` (4 cores de papel) agora usa os tokens semânticos `danger`/`info`/`violet`/`success` em vez de Tailwind cru, mantendo a mesma família de cor original. `userCapability()` (4 estados de capacidade operacional) também tokenizado. `AREA_META` (10 cores de área de prática) mantido intencionalmente cru — é categorização funcional real sem token equivalente no cheat-sheet, só a entrada `Outro` virou neutra. Diálogo de criar/editar usuário parou de reimplementar o `Dialog` primitive (removido gradiente decorativo + blur do header). Sem Cursor/Grok/Codex conectados — validado só pelo Lead (lint/build/480 testes limpos). Nenhuma lógica alterada: CRUD de usuários, merge com o quadro ORQESTRAI, troca de role via API, filtros — tudo intacto.
- **15/09/2026** — Fase 6, Administração lote A: Campos (`admin/campos/page.tsx`, `field-config-panel.tsx`) e Cláusulas (`admin/clausulas/page.tsx`, `clause-templates-admin-panel.tsx`) migrados. Removido um mapa de 13 cores cruas por tipo de campo (virou badge neutro único — cor sem significado de estado), dezenas de variações de opacidade de `primary-dark` convergidas para a escala neutra V2, estados reais de domínio (status de cláusula, obrigatoriedade, ativo/inativo) mapeados para os tokens semânticos da Fase 1, gradiente decorativo removido dos botões primários (herdam o `Button` primitive). `APP_USER_ROLE_LABELS` confirmado fora de escopo deste lote (só usado por Perfil, já migrado, e por Usuários, próximo sub-lote). Sem Cursor/Grok/Codex conectados — validado só pelo Lead (lint/build/480 testes limpos). Nenhuma lógica alterada: CRUD de campos/cláusulas, drag/reorder, validação de formulário e queries Supabase intactos.
- **15/09/2026** — Fase 6, lote 3: Documentos/Due diligence (`src/app/(crm)/crm/documentos/page.tsx`, `due-diligence-panel.tsx` ~1237 linhas, `due-timeline-ui.tsx` ~464 linhas) migrado — maior lote da Fase 6 até agora. Paleta de fase/estado (levantamento/compilação/revisão/finalizada, pontualidade do prazo) unificada nos 4 tokens semânticos da Fase 1 (`info`/`violet`/`warning`/`success`), sombra composta e hover-lift do card principal removidos, chips "filled" migrados de `primary-dark` para `brand-navy`. Cor de ícone por tipo de arquivo (PDF/PPT) deliberadamente mantida fora da paleta de tokens — é convenção de ícone, não estado semântico. Sem Cursor/Grok/Codex conectados — validado só pelo Lead (lint/build/480 testes limpos). Nenhuma lógica alterada: queries Supabase, `computeDuePunctuality`, `montarTimelineDueDiligence` e toda a matemática de prazo/cronologia intactas.
- **15/09/2026** — Fase 6, lote 2: Dashboard (`src/app/(crm)/crm/page.tsx`, `KpiCards`, `DashboardEtapaDistribution`, `IndicatorApprovalQueue`) migrado — KPIs unificados numa Surface só dividida em colunas (eram 4 cards com cor decorativa por KPI, violava §29.2), elevação decorativa (`hover:-translate-y-0.5`/`hover:shadow-xl`) removida das linhas de etapa/indicador. Sem Cursor/Grok/Codex conectados — validado só pelo Lead (lint/build/480 testes limpos). Nenhuma lógica alterada (queries do `SupabaseCrmRepository`, ações de aprovar/mesclar indicador intactas).
- **15/09/2026** — Fase 6, lote 1: `docs/DESIGN_SYSTEM_V2_CRM_BP.md` e `docs/DESIGN-SYSTEM-ATUAL.md` (existiam no disco desde 14/09 mas nunca haviam sido commitados) versionados primeiro. Depois, primeiro lote da Fase 6 — áreas pequenas e de baixo risco (Clientes: só trocou `heading-lg` legado por `text-v2-heading-lg`, componente já usava primitives V2 desde a Fase 2/3; Login: reescrita de `/login` e `LoginForm` removendo glass/blur/gradiente/blobs coloridos por §29.1; Perfil: `ProfileForm` parou de reimplementar estilo dos primitives; Notificações: `NotificationList` sem glass-card, cores de estado do Levantamento DUE tokenizadas). Sem Cursor/Grok/Codex conectados nesta sessão — trabalho e revisão feitos só pelo Lead. Validação: lint limpo, build limpo (todas as rotas compilam), 480/480 testes. Pendência registrada: `APP_USER_ROLE_LABELS` com cores cruas fica para o lote de Administração. Dashboard, Documentos (due diligence) e Administração (maior escopo, vários painéis) seguem pendentes.
- **14/09/2026** — Arquivo criado. Snapshot inicial registrado com base no estado observado: Fase 0 concluída, Fase 1 em andamento (tokens sendo escritos em `globals.css`, dois builds de validação rodando em paralelo — Claude e Codex, cada um independente).
- **14/09/2026** — Fase 1 concluída e commitada (`eeff1f8`). Codex revisou o diff de `globals.css`, achou e teve confirmado a correção de dois problemas reais antes de aprovar: (1) comentário CSS malformado (`*/` acidental no meio do texto, fechando o comentário cedo — corrigido); (2) contrato §7.2 incompleto (`--secondary-hover` e `--text-disabled` ausentes, quatro tokens renomeados sem necessidade — corrigido, valores canônicos restaurados). Veredito final: **aprovado com ressalvas** (não-bloqueantes, listadas acima). Validação (lint/typecheck/vitest 479 testes/build) rodada de forma independente pelos dois lados, resultado idêntico. Iniciando Fase 2 (Primitives) em seguida, por pedido do usuário.
- **14/09/2026** — Fase 2 concluída em dois lotes, ambos revisados pela Codex. Lote 1 (`882377d`): Button/Input/Select+CrmSelect/Badge/Card/Dialog+AlertDialog — achados corrigidos: `--text-placeholder` violava WCAG AA, variantes do Button (`inverse`/`teal`/`cta`/`secondary`/disabled) inconsistentes com §16; smoke test do contrato Dialog+Select promovido a teste permanente (`src/lib/ui/base-ui-select-dialog.test.tsx`). Lote 2 (`c4fdc82`): Alert/Textarea/Switch/Tabs/Table/Skeleton/Popover/Tooltip/Progress/CalendarBr/DateInputBr/TimeInputBr — achados corrigidos: Tabs sem a variante `segmented` do §20.1 (adicionada como alias de `default`), `TableRow` não alcançava 44px (`h-11` adicionado), e um achado sistêmico importante — `tailwind-merge` não resolve corretamente duas classes `rounded-[var(--x)]`/`shadow-[var(--x)]` como conflitantes, deixando um `className` de override de consumidor silenciosamente inoperante (achado no `PopoverContent` de `sidebar-account-menu.tsx`); todas as 27 ocorrências (dos dois lotes) trocadas pela sintaxe curta `rounded-(--x)`/`shadow-(--x)` do Tailwind v4, confirmada com teste direto do `twMerge`. Veredito final do lote 2: **aprovado, sem ressalvas**. Checkbox/RadioGroup não existem como componentes no repo; Avatar/Label revisados e mantidos como estão. Validação idêntica dos dois lados em ambos os lotes.
- **14/09/2026** — Fase 3 (Shell), lote 1: AppShell (sidebar 72/248px, item ativo, z-index formalizado), CrmPageHeader (removido o card de 28px de raio — exemplo textual direto do §14.4), CrmSurfaceHeader. Codex achou 1 bloqueador real (`overflow-hidden` sobrando no novo `CrmPageHeader` cortava o ring de foco dos botões de `actions`, corrigido) e confirmou duas dúvidas técnicas levantadas por mim (ícone da sidebar recolhida cabe sem corte a 72px; `before:` pseudo-elemento não precisa de `content-['']` explícito, Tailwind já inicializa `--tw-content: ""` globalmente). Duas ressalvas não-bloqueantes registradas acima (header mobile ~64px vs 56-60px do §14.3; hex crus restantes no AppShell). Verificação visual em browser autenticado não foi possível neste lote (sem credenciais de teste) — registrado como lacuna, não omitido. Codex terminou a revisão perto do limite de sessão (~5%); não pedi rodada extra depois dos dois ajustes finais (comentário desatualizado dizendo "88px", este changelog) — apliquei e validei sozinho (lint/typecheck/480 testes/build, um teste falhou uma vez por instabilidade do ambiente e passou limpo na repetição).
- **14/09/2026** — Fase 3 (Shell), lote 2 — **fecha a fase**: header mobile do AppShell 64px→60px; `SidebarAccountMenu` e `CrmNotificationsBell` (menu de conta e sino de notificações, ambos consumidos pelo AppShell) totalmente tokenizados — dezenas de hex crus (`#e1e5eb`, `#102033`, `#eef5ff`, `#fafbfd`, `#e6eaf2` etc.) trocados pelos tokens V2 via passe com `sed` seguido de revisão manual dos casos que não bateram no padrão simples (sombra composta com cor inline, blur/translucidez do popover). Codex já sem budget de sessão (~5%, prompt de troca de modelo) — segui a decisão do usuário ("continuar mesmo assim, sem Codex se necessário") e fiz a verificação eu mesmo: sweep por classes legadas remanescentes (zero encontradas), e uma inspeção mais funda depois que a checagem usual via `.next/static/chunks/*.css` deu falso-negativo para classes do `CrmNotificationsBell` — nota de metodologia registrada acima (rotas dinâmicas do Next 16/Turbopack não garantem que o CSS de um componente apareça nesses 3 arquivos; a verificação confiável é compilar a classe direto via `@tailwindcss/postcss`, não depender do output do bundler). `CrmToolbar`/`CrmEntityHeader` deliberadamente não criados agora (sem consumidor real ainda — ver Snapshot). Validação: lint/typecheck/build limpos; vitest com uma falha intermitente não relacionada (lógica de proposta, `src/lib/crm`, nada a ver com este lote) que passou limpo em reruns subsequentes — registrado como instabilidade de ambiente, não regressão.
- **14/09/2026** — Fase 4 (Leads) lote visual pelo Grok — Leads/Kanban: toolbar (SubtleSurface, filtros 36px, busca/seleção azul), colunas/cards sem glass nem hover-lift, overlay DnD com `shadow-v2-lg`, Novo Lead no Dialog global (max 1120px, campos 40px, header claro, Inter removida do fluxo), shell da ficha com `CrmEntityHeader` + tabs `line`. Conteúdo das tabs Proposta/Contrato não tocado. Pedido de revisão à Codex.
- **14/09/2026** — Fase 4 **fecha**: revisão em duas camadas. Eu (Lead) validei primeiro — lint/typecheck/480 testes/build limpos, sem regressão de bracket-syntax, sem sobra de Inter, hero escuro removido, campos de 44px do Novo Lead corrigidos, DnD/skeleton/silent-refresh/retry/toggle RD Station (localStorage `crm.kanban.prefs.v1`) todos confirmados intactos. Depois pedi revisão independente à Codex (18min de trabalho, budget recuperado): **bloqueado por 1 achado real, não regressão cosmética** — em `lead-detail-view.tsx`, nome e avatar do "Responsável" no `CrmEntityHeader` vinham de fontes independentes (`lead.ownerUserName`/`ownerUserAvatarUrl`, que só existem populados na listagem via mapeamento de e-mail do RD — `src/app/api/crm/leads/route.ts` — e nunca no loader desta página de detalhe, `getLeadById`), com risco real de combinar nome de uma pessoa com avatar de outra; a versão pré-migração usava uma única fonte (`cadastradoPor.resolvedUser`) para os dois. **Corrigido** revertendo para essa fonte única — comportamento idêntico ao pré-migração, sem tocar em nenhum loader/rota (evita expandir escopo pra dentro de `src/app/api/` ou `src/modules/`). Também corrigido um achado menor: o rótulo "Atualização" aparecia mesmo quando o valor exibido era a data de *criação* (sem `atualizadoEm`) — rótulo agora acompanha a fonte real ("Atualização" vs "Criação"). Codex confirmou positivamente: nenhuma regressão em DnD/filtros/busca/persistência, `zIndex={130}` bate com `--z-drag-overlay`, o teste permanente Dialog+Select passou e ela escreveu testes isolados extras confirmando que o picker customizado do Novo Lead e o AlertDialog de confirmação não fecham o Dialog pai, `CrmEntityHeader` tem API adequada e fiel ao §29.4. Ressalvas não-bloqueantes pra um lote futuro (arquivos da própria Grok — Leads/Kanban): foco no primeiro campo inválido do Novo Lead ainda não implementado (§29.5 — dívida pré-existente, não regressão); superfícies com raio/sombra legados em `new-demand-form.tsx` (linhas ~312, 1094, 1303); cores Tailwind cruas de estado ainda em `pipeline-lead-card-content.tsx`/`pipeline-stage-panels.tsx`/`pipeline-kanban-panel-shell.tsx` (mapear pra success/warning/danger/info). Validação final (com as duas correções): lint/typecheck/480 testes/build limpos.
- **14/09/2026** — Fase 5 (Propostas/Contratos) **fecha** — primeiro lote da Grok — Propostas/Contratos, área jurídica mais sensível do produto até aqui. Novo `src/components/crm/document-builder-chrome.tsx`: chrome compartilhado dos dois builders — `DocumentSaveStatus` (Salvo/Salvando/Alterações não salvas, tokens `success`/`warning`), `DocumentBuilderHubHeader`, `DocumentMobilePaneToggle` (editar/visualizar mobile, reusa o padrão `segmented` do `CrmSurfaceHeader`), `DocumentBuilderDialogHeader`, `DocumentStatusCard` (tons ok/warn/neutral), `DocumentInheritedBadge` ("Da proposta", §29.9, tokens `info`). Aplicado em `proposta-document-builder.tsx`, `contrato-document-builder.tsx` e mais 7 arquivos de apoio (escopo, investimento, painéis do objeto contratual, billing). Revisão em duas camadas antes de fechar: eu (Lead) confirmei primeiro que `CONTRACT_BODY_STYLE` (fonte/margens do preview jurídico) não aparece em nenhum hunk do diff, nenhum endpoint de geração de documento foi tocado, e o contrato Dialog+Select (`modal={false}` + `isInteractionFromBaseUiSelectLayer`) permanece intacto nos dois builders — depois pedi revisão independente à Codex, com ênfase explícita em confirmar ausência de mudança jurídica/de cálculo. Codex (8min30) **bloqueou por 1 achado real, de layout**: `flex-1` sem guarda `md:` competia com `md:w-[46%]`/`md:w-[54%]` nas colunas do editor split-pane (usado pra alternar edição/preview no mobile), fazendo o desktop tender a 50/50 em vez das proporções corretas — **corrigido** com `md:flex-none` nas três colunas afetadas (dois builders). Confirmou positivamente, comparando estruturalmente contra o HEAD: cláusulas, objeto contratual, valores, pins, signatários, geração DOCX/PDF, `CONTRACT_BODY_STYLE`/Times New Roman/margens e a chave de dado `color: "teal"` dos signatários (só a representação visual mudou) — tudo intacto; nenhum arquivo de API ou módulo de negócio alterado. Ressalva não-bloqueante corrigida: `DocumentMobilePaneToggle` usava semântica de `tablist`/`tab` sem implementar o contrato completo (sem `aria-controls`/`tabpanel`/navegação por setas) — trocado por `role="group"` + `aria-pressed`, fiel a um segmented control simples. `git diff --check` também apontou uma linha em branco sobrando (CRLF) no fim de `proposta-document-builder.tsx`, corrigida. Segunda rodada da Codex, com leitura fresca de cada correção: **aprovado, sem ressalvas**. Validação (lint/typecheck/480 testes/build) idêntica dos dois lados, nas duas rodadas. Hub `/crm/contratos` (fora do fluxo do lead) deliberadamente não migrado neste lote.
